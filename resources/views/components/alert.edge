<div class="alert alert-{{ type }} {{ dismissible ? 'alert-dismissible fade show' : null }}" role="alert">
  @if(dismissible)
    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
      <span aria-hidden="true">&times;</span>
    </button>
  @endif

  @!yield($slot.main)
</div>
